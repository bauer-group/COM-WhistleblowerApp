"""Hinweisgebersystem – API Package.

Top-level package for all API version modules.  Each version
(``v1``, ``v2``, ...) is a sub-package that aggregates its own
routers via an ``APIRouter`` instance.
"""
